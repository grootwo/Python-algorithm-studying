def solution(board, moves):
    stack = deque([])
    count = 0
    for move in moves:
        
    return count

def get_top_doll(col, board):
    for i in range(len(board)):
        if board[i][row] != 0:
            return board[i][row]
    return 0
